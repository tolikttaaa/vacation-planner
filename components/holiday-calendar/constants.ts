// Shared UI tokens for the holiday calendar layout.

export const PANEL_STYLES = {
  container: "bg-card border-2 border-border rounded-xl shadow-sm w-full min-w-[600px] panel-neon",
  maxWidth: "max-w-[1560px]",
  minWidth: "min-w-[600px]",
} as const
